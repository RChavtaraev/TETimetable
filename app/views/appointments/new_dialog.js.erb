$("#new_modal-window").html("<%= escape_javascript(render 'appointments/new_dialog') %>");

$('div.expandable').expander({
  slicePoint:       0,
  expandText: 'подробнее...',
  userCollapseText: 'скрыть подробности'
});

function clr()
{
  if ($('#id').val() != '') {
    $('#id').val("");
    $('#email').val("");
    $('#phone').val("");
    $('#birth_date').val("");
    $('#address').val("");
    $('#comment').val("");
  }
}
